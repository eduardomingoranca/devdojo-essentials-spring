server:
  error:
    include-stacktrace: on_param

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_essentials?useSSL=false
    userName: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update

management:
  endpoints:
    web:
      exposure:
        include: "*"

info:
  app: Spring Boot Project Essentials
  github: https://github.com/eduardomingoranca/essentials-spring-boot
